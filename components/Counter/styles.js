import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    container: {
      flexDirection: "row",
      alignContent: "center",
      alignItems: "center",
      justifyContent: "center",
    },
    count: {
      width: 100,
      textAlign: "center",
    },
    field: {
      width: 150,
      textAlign: "left",
      fontWeight: "bold"
    },
  });
